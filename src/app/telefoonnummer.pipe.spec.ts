import { TelefoonnummerPipe } from './telefoonnummer.pipe';

describe('TelefoonnummerPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoonnummerPipe();
    expect(pipe).toBeTruthy();
  });
});
